# 🎨 기존 GUI와 고급 조건 통합 방안

## 📋 현재 GUI 구조 분석

### 기존 스크리닝 조건

**매수 조건 (4개):**
```
✓ 최근 60일선이 120일선 돌파 + 우상향 + 이평선 터치
✓ 볼린저밴드 하단 터치 + RSI < 35
✓ MACD 골든 크로스 + 거래량 증가
✓ 20일 상대강도 상승 + 펀더멘털 양호
```

**매도 조건 (4개):**
```
✓ 데드크로스 + 60일선 3% 하향이탈
✓ 20% 수익달성 또는 -7% 손절
✓ 볼린저 상단 + RSI > 70
✓ 거래량 급감 + 모멘텀 약화
```

---

## 🎯 통합 방안 3가지

### 방안 1: 탭 추가 (추천) ⭐⭐⭐⭐⭐

**기존 조건과 고급 조건을 탭으로 분리**

#### 장점
- ✅ 기존 사용자 혼란 없음
- ✅ 깔끔한 UI
- ✅ 단계적 학습 가능

#### 단점
- 탭 전환 필요

#### GUI 디자인

```
┌────────────────────────────────────────────────────────────┐
│  Stock Screener                                    [_][□][X]│
├────────────────────────────────────────────────────────────┤
│                                                             │
│  [🔍 종목 검색]  [📊 기본 스크리닝]  [🚀 고급 스크리닝]    │
│  ─────────────────────────────────────────────────────     │
│                                                             │
│  ┌─ 🚀 고급 스크리닝 ─────────────────────────────┐       │
│  │                                                  │       │
│  │  전략 프리셋:                                    │       │
│  │  [균형형 ▼] ⭐ 추천                              │       │
│  │   • 안전 중시형 (승률 75%, 연수익 18-22%)        │       │
│  │   • 균형형 (승률 70%, 연수익 25-30%) ⭐          │       │
│  │   • 공격형 (승률 65%, 연수익 30-40%)            │       │
│  │   • 커스텀 (직접 설정)                           │       │
│  │                                                  │       │
│  │  ─────────────────────────────────────          │       │
│  │                                                  │       │
│  │  활성화된 조건: (클릭하여 상세 설정)             │       │
│  │                                                  │       │
│  │  ✓ 다중 시간대 확인        [⚙️]                │       │
│  │  ✓ 시장 강도 필터          [⚙️]                │       │
│  │  ✓ 상대 강도 비교          [⚙️]                │       │
│  │  ✓ 지지/저항선 감지        [⚙️]                │       │
│  │  ✗ 적응형 전략             [⚙️]                │       │
│  │  ✗ 기관 매집 감지          [⚙️]                │       │
│  │                                                  │       │
│  │  ─────────────────────────────────────          │       │
│  │                                                  │       │
│  │  📊 예상 성과:                                   │       │
│  │    • 승률: 70-75%                                │       │
│  │    • 연수익: 25-30%                              │       │
│  │    • Max DD: -8%                                 │       │
│  │    • 월 거래 빈도: 4-6회                         │       │
│  │                                                  │       │
│  │  [프리셋 저장]  [백테스트]  [고급 스크리닝 시작] │       │
│  └──────────────────────────────────────────────┘       │
│                                                             │
│  📊 스크리닝 결과 (0개)                                     │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  (결과 테이블)                                        │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

---

### 방안 2: 그룹박스 추가

**기존 GUI 아래에 "고급 필터" 그룹박스 추가**

#### 장점
- ✅ 한 화면에서 모든 조건 확인
- ✅ 즉시 비교 가능

#### 단점
- 화면이 길어짐
- 복잡해 보일 수 있음

#### GUI 디자인

```
┌────────────────────────────────────────────────────────────┐
│  Stock Screener                                             │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─ 💰 기본 매수 조건 ──────┐ ┌─ 📉 기본 매도 조건 ─────┐ │
│  │ ✓ MA 돌파               │ │ ✓ 데드크로스           │ │
│  │ ✓ BB 하단 + RSI         │ │ ✓ 수익/손절            │ │
│  │ ✓ MACD 골든크로스       │ │ ✓ BB 상단 + RSI        │ │
│  │ ✓ 모멘텀 상승           │ │ ✓ 거래량 급감          │ │
│  └─────────────────────┘ └────────────────────────┘ │
│                                                             │
│  ┌─ 🚀 고급 필터 (선택사항) ──────────────────────────┐    │
│  │                                                     │    │
│  │  빠른 선택: [균형형 ▼]  [적용]                      │    │
│  │                                                     │    │
│  │  ─────────────────────────────────────             │    │
│  │                                                     │    │
│  │  ☑ 다중 시간대 확인 (승률 +17%p)                    │    │
│  │  ☑ 시장 강도 필터 (손실 -50%)                       │    │
│  │  ☑ 상대 강도 비교 (수익 +30%)                       │    │
│  │  ☐ 지지/저항선 감지                                 │    │
│  │  ☐ 적응형 전략                                      │    │
│  │                                                     │    │
│  │  [전체 선택]  [전체 해제]  [상세 설정]              │    │
│  └─────────────────────────────────────────────────┘    │
│                                                             │
│  [🔍 스크리닝 시작]  [⏹️ 중지]  [📈 백테스트]  [📊 저장]   │
└────────────────────────────────────────────────────────────┘
```

---

### 방안 3: 새 창 열기

**"고급 설정" 버튼으로 별도 다이얼로그 열기**

#### 장점
- ✅ 기존 UI 유지
- ✅ 필요할 때만 사용

#### 단점
- 창 관리 번거로움
- 통합감 낮음

#### GUI 디자인

**메인 화면:**
```
┌────────────────────────────────────────────────┐
│  ┌─ 💰 기본 매수 조건 ──┐ ┌─ 📉 기본 매도 조건 │
│  │ (기존 그대로)        │ │ (기존 그대로)     │
│  └──────────────────┘ └────────────────── │
│                                             │
│  [🚀 고급 설정]  [🔍 스크리닝]  [📊 저장]    │
└────────────────────────────────────────────────┘
```

**"고급 설정" 클릭 시 팝업:**
```
┌───────────────────────────────────────┐
│  🚀 고급 스크리닝 설정                 │
├───────────────────────────────────────┤
│  (프리셋 및 조건 선택)                │
│  [확인]  [취소]                       │
└───────────────────────────────────────┘
```

---

## 🏆 최종 추천: 방안 1 (탭 방식)

### 이유

1. **사용자 경험** ⭐⭐⭐⭐⭐
   - 기존 사용자: "기본 스크리닝" 탭 계속 사용
   - 신규 기능: "고급 스크리닝" 탭으로 자연스럽게 이동
   - 혼란 없음

2. **UI 깔끔함** ⭐⭐⭐⭐⭐
   - 화면 분리로 복잡도 낮음
   - 각 탭이 명확한 목적

3. **확장성** ⭐⭐⭐⭐⭐
   - 추후 더 많은 탭 추가 가능
   - 모듈화 쉬움

4. **학습 곡선** ⭐⭐⭐⭐⭐
   - 초보자: 기본 탭만 사용
   - 중급자: 고급 탭 탐색
   - 점진적 학습

---

## 📐 상세 구현 설계 (탭 방식)

### 탭 구조

```python
class StockScreener(QMainWindow):
    def __init__(self):
        super().__init__()

        # 메인 탭 위젯
        self.tabs = QTabWidget()

        # 1. 종목 검색 탭 (기존)
        self.search_tab = self.create_search_tab()
        self.tabs.addTab(self.search_tab, "🔍 종목 검색")

        # 2. 기본 스크리닝 탭 (기존 조건들)
        self.basic_screening_tab = self.create_basic_screening_tab()
        self.tabs.addTab(self.basic_screening_tab, "📊 기본 스크리닝")

        # 3. 고급 스크리닝 탭 (새로운!)
        self.advanced_screening_tab = self.create_advanced_screening_tab()
        self.tabs.addTab(self.advanced_screening_tab, "🚀 고급 스크리닝")

        self.setCentralWidget(self.tabs)
```

### 기본 스크리닝 탭 (기존 유지)

```python
def create_basic_screening_tab(self):
    """기본 스크리닝 탭 생성 (기존 코드 거의 그대로)"""

    widget = QWidget()
    layout = QVBoxLayout()

    # 기존 매수/매도 조건 그룹박스
    buy_group = QGroupBox("💰 기본 매수 조건")
    # ... (기존 코드)

    sell_group = QGroupBox("📉 기본 매도 조건")
    # ... (기존 코드)

    # 버튼들
    button_layout = QHBoxLayout()
    self.basic_screen_btn = QPushButton("🔍 기본 스크리닝 시작")
    self.basic_screen_btn.clicked.connect(self.run_basic_screening)
    # ... (기존 코드)

    widget.setLayout(layout)
    return widget
```

### 고급 스크리닝 탭 (새로운!)

```python
def create_advanced_screening_tab(self):
    """고급 스크리닝 탭 생성"""

    widget = QWidget()
    layout = QVBoxLayout()

    # 1. 프리셋 선택
    preset_group = QGroupBox("📋 전략 프리셋")
    preset_layout = QVBoxLayout()

    self.preset_combo = QComboBox()
    self.preset_combo.addItems([
        "균형형 (추천) ⭐",
        "안전 중시형",
        "공격형",
        "커스텀"
    ])
    self.preset_combo.currentTextChanged.connect(self.on_preset_changed)

    # 프리셋 설명 레이블
    self.preset_desc = QLabel()
    self.preset_desc.setWordWrap(True)
    self.update_preset_description()

    preset_layout.addWidget(QLabel("전략 선택:"))
    preset_layout.addWidget(self.preset_combo)
    preset_layout.addWidget(self.preset_desc)
    preset_group.setLayout(preset_layout)
    layout.addWidget(preset_group)

    # 2. 고급 조건 체크박스
    conditions_group = QGroupBox("🔧 고급 조건")
    conditions_layout = QGridLayout()

    self.multi_timeframe_check = QCheckBox("다중 시간대 확인")
    self.multi_timeframe_check.setToolTip("주봉/월봉 추세 확인 (승률 +17%p)")
    conditions_layout.addWidget(self.multi_timeframe_check, 0, 0)

    self.multi_timeframe_btn = QPushButton("⚙️")
    self.multi_timeframe_btn.setMaximumWidth(40)
    self.multi_timeframe_btn.clicked.connect(self.open_multi_timeframe_settings)
    conditions_layout.addWidget(self.multi_timeframe_btn, 0, 1)

    self.market_strength_check = QCheckBox("시장 강도 필터")
    self.market_strength_check.setToolTip("S&P500 추세/VIX 확인 (손실 -50%)")
    conditions_layout.addWidget(self.market_strength_check, 1, 0)

    self.market_strength_btn = QPushButton("⚙️")
    self.market_strength_btn.setMaximumWidth(40)
    conditions_layout.addWidget(self.market_strength_btn, 1, 1)

    self.relative_strength_check = QCheckBox("상대 강도 비교")
    self.relative_strength_check.setToolTip("섹터 내 강도 비교 (수익 +30%)")
    conditions_layout.addWidget(self.relative_strength_check, 2, 0)

    self.relative_strength_btn = QPushButton("⚙️")
    self.relative_strength_btn.setMaximumWidth(40)
    conditions_layout.addWidget(self.relative_strength_btn, 2, 1)

    # ... 추가 조건들

    conditions_group.setLayout(conditions_layout)
    layout.addWidget(conditions_group)

    # 3. 예상 성과 표시
    performance_group = QGroupBox("📊 예상 성과")
    performance_layout = QVBoxLayout()

    self.performance_label = QLabel()
    self.update_performance_estimate()

    performance_layout.addWidget(self.performance_label)
    performance_group.setLayout(performance_layout)
    layout.addWidget(performance_group)

    # 4. 버튼들
    button_layout = QHBoxLayout()

    self.save_preset_btn = QPushButton("💾 프리셋 저장")
    self.save_preset_btn.clicked.connect(self.save_custom_preset)
    button_layout.addWidget(self.save_preset_btn)

    self.backtest_advanced_btn = QPushButton("📈 백테스트")
    self.backtest_advanced_btn.clicked.connect(self.run_advanced_backtest)
    button_layout.addWidget(self.backtest_advanced_btn)

    self.advanced_screen_btn = QPushButton("🚀 고급 스크리닝 시작")
    self.advanced_screen_btn.clicked.connect(self.run_advanced_screening)
    self.advanced_screen_btn.setStyleSheet(
        "QPushButton { background-color: #9C27B0; color: white; "
        "font-weight: bold; padding: 12px; font-size: 14px; }"
    )
    button_layout.addWidget(self.advanced_screen_btn)

    layout.addLayout(button_layout)

    widget.setLayout(layout)
    return widget
```

---

## 🔧 주요 메서드 구현

### 프리셋 변경 시

```python
def on_preset_changed(self, preset_name):
    """프리셋 선택 시 조건 자동 설정"""

    if "균형형" in preset_name:
        # 균형형 프리셋
        self.multi_timeframe_check.setChecked(True)
        self.market_strength_check.setChecked(True)
        self.relative_strength_check.setChecked(True)
        self.support_resistance_check.setChecked(True)
        self.adaptive_strategy_check.setChecked(True)
        self.volume_profile_check.setChecked(True)
        self.accumulation_check.setChecked(False)
        self.candlestick_check.setChecked(False)

    elif "안전" in preset_name:
        # 안전 중시형 프리셋
        self.multi_timeframe_check.setChecked(True)
        self.market_strength_check.setChecked(True)
        self.relative_strength_check.setChecked(False)
        self.support_resistance_check.setChecked(True)
        self.adaptive_strategy_check.setChecked(False)
        # ... 모든 조건 OFF

    elif "공격형" in preset_name:
        # 공격형 프리셋
        self.accumulation_check.setChecked(True)
        self.relative_strength_check.setChecked(True)
        self.candlestick_check.setChecked(True)
        # ...

    else:  # 커스텀
        # 모든 조건 사용자가 직접 선택
        pass

    # 설명 및 예상 성과 업데이트
    self.update_preset_description()
    self.update_performance_estimate()
```

### 고급 스크리닝 실행

```python
def run_advanced_screening(self):
    """고급 스크리닝 실행"""

    try:
        # 1. 활성화된 조건 수집
        active_conditions = []

        if self.multi_timeframe_check.isChecked():
            active_conditions.append('multi_timeframe')

        if self.market_strength_check.isChecked():
            active_conditions.append('market_strength')

        # ... 다른 조건들

        # 2. 스크리닝 실행
        self.advanced_screen_btn.setEnabled(False)
        self.statusbar.showMessage('🚀 고급 스크리닝 중...')

        # 워커 쓰레드로 실행 (GUI 블로킹 방지)
        from advanced_screening_worker import AdvancedScreeningWorker

        self.worker = AdvancedScreeningWorker(
            active_conditions=active_conditions,
            preset=self.preset_combo.currentText()
        )
        self.worker.progress.connect(self.update_progress)
        self.worker.finished.connect(self.on_advanced_screening_finished)
        self.worker.start()

    except Exception as e:
        QMessageBox.critical(self, "오류", f"고급 스크리닝 중 오류: {str(e)}")
```

---

## 📊 결과 표시

### 기본 vs 고급 결과 비교

```
┌────────────────────────────────────────────────────────────┐
│  📊 스크리닝 결과 비교                                      │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  [기본 스크리닝]  [고급 스크리닝]  [비교]                   │
│                                                             │
│  ┌─ 고급 스크리닝 결과 (12개) ──────────────────────┐      │
│  │                                                   │      │
│  │  종목   가격   신호           신뢰도  예상수익     │      │
│  │  ─────────────────────────────────────────      │      │
│  │  삼성   102K  균형형매수       85%    +28%       │      │
│  │  SK하   145K  균형형매수       78%    +25%       │      │
│  │  NAVER  240K  균형형매수       82%    +30%       │      │
│  │  ...                                              │      │
│  │                                                   │      │
│  │  평균 신뢰도: 81%                                 │      │
│  │  예상 승률: 72%                                   │      │
│  │                                                   │      │
│  └───────────────────────────────────────────────┘      │
│                                                             │
│  [엑셀로 저장]  [차트 보기]  [백테스트 비교]                │
└────────────────────────────────────────────────────────────┘
```

---

## 💾 설정 저장

### 사용자 프리셋 저장

```python
def save_custom_preset(self):
    """사용자 정의 프리셋 저장"""

    name, ok = QInputDialog.getText(
        self, "프리셋 저장", "프리셋 이름을 입력하세요:"
    )

    if ok and name:
        preset_data = {
            'name': name,
            'created_date': datetime.now().isoformat(),
            'conditions': {
                'multi_timeframe': self.multi_timeframe_check.isChecked(),
                'market_strength': self.market_strength_check.isChecked(),
                'relative_strength': self.relative_strength_check.isChecked(),
                # ... 모든 조건
            },
            'settings': {
                # 각 조건의 세부 설정값
            }
        }

        # JSON 파일로 저장
        preset_dir = Path("presets")
        preset_dir.mkdir(exist_ok=True)

        file_path = preset_dir / f"{name}.json"
        with open(file_path, 'w', encoding='utf-8') as f:
            json.dump(preset_data, f, ensure_ascii=False, indent=2)

        QMessageBox.information(
            self, "저장 완료",
            f"프리셋 '{name}'이(가) 저장되었습니다."
        )

        # 콤보박스에 추가
        self.preset_combo.addItem(f"{name} (사용자)")
```

---

## 🎯 마이그레이션 계획

### Phase 1: 탭 구조 추가 (1주)
```python
✅ 기존 UI를 "기본 스크리닝" 탭으로 이동
✅ "고급 스크리닝" 탭 뼈대 생성
✅ 프리셋 3개 추가
✅ 기본 체크박스 UI
```

### Phase 2: 고급 조건 통합 (2주)
```python
✅ Phase 1
+ 각 고급 조건 구현
+ 설정 다이얼로그
+ 프리셋 저장/불러오기
```

### Phase 3: 백테스트 & 비교 (1주)
```python
✅ Phase 2
+ 고급 조건 백테스트
+ 기본 vs 고급 비교 기능
+ 성과 시각화
```

---

## 🔄 기존 코드 최소 변경

### 변경 사항 요약

**기존 코드 (95% 유지):**
- `create_search_group()` → `create_search_tab()`로 이름 변경
- `create_screening_group()` → `create_basic_screening_tab()`로 변경
- 나머지 로직 그대로 유지

**새로운 코드 (추가):**
- `create_advanced_screening_tab()` 신규 생성
- `run_advanced_screening()` 신규 생성
- `AdvancedScreeningWorker` 클래스 신규

**메인 변경:**
```python
# Before
def __init__(self):
    main_widget = QWidget()
    main_layout = QVBoxLayout()
    main_layout.addWidget(self.create_search_group())
    main_layout.addWidget(self.create_screening_group())

# After
def __init__(self):
    self.tabs = QTabWidget()
    self.tabs.addTab(self.create_search_tab(), "🔍 종목 검색")
    self.tabs.addTab(self.create_basic_screening_tab(), "📊 기본 스크리닝")
    self.tabs.addTab(self.create_advanced_screening_tab(), "🚀 고급 스크리닝")
    self.setCentralWidget(self.tabs)
```

---

## 🎉 최종 결과 미리보기

```
┌────────────────────────────────────────────────────────────┐
│  Stock Screener - AI Enhanced                      [_][□][X]│
├────────────────────────────────────────────────────────────┤
│  [🔍 종목 검색]  [📊 기본 스크리닝]  [🚀 고급 스크리닝] ← 탭│
│  ════════════════════════════════════════════════════════  │
│                                                             │
│  현재 탭: 🚀 고급 스크리닝                                  │
│                                                             │
│  ┌─ 전략 프리셋 ────────────────────────────────┐         │
│  │  [균형형 (추천) ⭐ ▼]                          │         │
│  │  승률 70-75% | 연수익 25-30% | Max DD -8%    │         │
│  └───────────────────────────────────────────┘         │
│                                                             │
│  ┌─ 활성화된 조건 ──────────────────────────────┐         │
│  │  ✓ 다중 시간대 확인     [⚙️]                 │         │
│  │  ✓ 시장 강도 필터       [⚙️]                 │         │
│  │  ✓ 상대 강도 비교       [⚙️]                 │         │
│  │  ✓ 지지/저항선 감지     [⚙️]                 │         │
│  └───────────────────────────────────────────┘         │
│                                                             │
│  [💾 저장]  [📈 백테스트]  [🚀 고급 스크리닝 시작]        │
│                                                             │
│  📊 스크리닝 결과 (12개) - 평균 신뢰도 81%                  │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ 종목    가격    신호         신뢰도  예상수익          │ │
│  │ ────────────────────────────────────────────        │ │
│  │ 삼성    102K   균형형매수     85%    +28%            │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

---

## 🎯 결론

**추천: 탭 방식으로 통합** ⭐⭐⭐⭐⭐

**이유:**
1. ✅ 기존 사용자 경험 유지
2. ✅ 새로운 기능 명확히 분리
3. ✅ 코드 변경 최소화 (95% 유지)
4. ✅ 확장성 우수
5. ✅ UI 깔끔

**구현 시작하시겠습니까?** Phase 1부터 하면 1주일 안에 탭 구조 완성됩니다! 💪

---

**작성일**: 2025-10-28
**작성자**: Claude Code Assistant
**UI/UX**: 최소 변경, 최대 효과
**추천도**: ⭐⭐⭐⭐⭐
